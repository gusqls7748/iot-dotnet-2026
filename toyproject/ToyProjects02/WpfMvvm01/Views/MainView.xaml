<Window x:Class="WpfMvvm01.Views.MainView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfMvvm01.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainView">

    <StackPanel>
        <TextBox FontSize="30" Text="{Binding Message, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock FontSize="30" Foreground="Blue" Text="{Binding Message}" />

        <ListView ItemsSource="{Binding People}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" FontSize="20" Foreground="Aquamarine" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <TextBlock FontSize="20" Foreground="CadetBlue" Text="{Binding SelectedPerson}"
    </StackPanel>
</Window>
